"""Service layer: business logic shared by the REST server and the MCP server.

This is where queries against the lenses live. Both `server.py` (HTTP) and
`mcp_server.py` (MCP/stdio) call into this module so they stay consistent.

Stub. Real service functions land in the next phase.
"""

from __future__ import annotations

# Placeholder — implementation follows in Day 2 of the MVP plan.
